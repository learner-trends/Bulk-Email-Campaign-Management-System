spring:
  application:
    name: bulk-email-campaign

  datasource:
    url: jdbc:mysql://mysql:3306/bulk_email_campaign?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: testersaho@gmail.com
    password: etiq upzg nuiy btdc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  task:
    scheduling:
      pool:
        size: 5

  thymeleaf:
    cache: false
    encoding: UTF-8

server:
  port: 8080

app:
  mail:
    from: your-email@gmail.com
    from-name: Campaign Manager

logging:
  level:
    com.enterprise.emailcampaign: INFO
    org.springframework.mail: DEBUG

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"